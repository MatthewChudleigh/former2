'use strict';

/**
 * Render cost estimates as a CLI table.
 * @param {import('../estimator').CostEstimate[]} estimates
 * @param {object} [options]
 * @returns {string} Formatted table string
 */
function renderTable(estimates, options = {}) {
    // TODO: implement table rendering
    throw new Error('Not implemented');
}

module.exports = {
    renderTable,
};

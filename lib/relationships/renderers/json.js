'use strict';

/**
 * Render a resource graph as a JSON structure.
 * @param {import('../graph-builder').Graph} graph
 * @param {object} [options]
 * @returns {string} JSON string with { nodes, edges }
 */
function renderJson(graph, options = {}) {
    // TODO: implement JSON rendering
    throw new Error('Not implemented');
}

module.exports = {
    renderJson,
};

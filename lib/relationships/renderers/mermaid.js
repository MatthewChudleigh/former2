'use strict';

/**
 * Render a resource graph as Mermaid diagram syntax.
 * @param {import('../graph-builder').Graph} graph
 * @param {object} [options]
 * @returns {string} Mermaid format string
 */
function renderMermaid(graph, options = {}) {
    // TODO: implement Mermaid rendering
    throw new Error('Not implemented');
}

module.exports = {
    renderMermaid,
};
